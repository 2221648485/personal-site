---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { GITHUB_URL, SITE_AUTHOR, SITE_DESCRIPTION, SITE_EMAIL, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`关于 | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="about-panel">
				<p class="eyebrow">About</p>
				<h1>关于我</h1>
				<p>
					这里暂时先保留为一个简洁版本。你后面可以把姓名、学校、研究方向、感兴趣的话题、联系方式和常用链接逐步补进来。
				</p>
				<hr />
				<div class="facts">
					<div>
						<h2>当前身份</h2>
						<p>{SITE_AUTHOR}，计算机研究生。</p>
					</div>
					<div>
						<h2>这个站点想做什么</h2>
						<p>把碎片化的学习、生活和思考整理成能长期积累的中文内容。</p>
					</div>
					<div>
						<h2>联系方式</h2>
						<p>邮箱：{SITE_EMAIL}</p>
						<p>
							GitHub：
							<a href={GITHUB_URL} target="_blank" rel="noreferrer">{GITHUB_URL}</a>
						</p>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.about-panel {
		margin-top: 1rem;
		padding: 2rem;
		border: 1px solid rgba(var(--border), 0.85);
		border-radius: 30px;
		background: rgba(var(--panel), 0.76);
		box-shadow: var(--shadow);
	}

	.eyebrow {
		margin-bottom: 0.8rem;
		font-size: 0.78rem;
		letter-spacing: 0.16em;
		text-transform: uppercase;
		color: rgb(var(--muted));
	}

	.facts {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
	}

	.facts h2 {
		font-size: 1.15rem;
	}

	@media (max-width: 900px) {
		.facts {
			grid-template-columns: 1fr;
		}
	}
</style>
